const { Pool } = require('pg'); require('dotenv').config(); const pool = new Pool({ connectionString: process.env.DATABASE_URL, ssl: { rejectUnauthorized: false } }); async function run() { const client = await pool.connect(); try { await client.query('BEGIN'); const r1 = await client.query('UPDATE zalish_invoices SET "date" = created_at::date;'); const r2 = await client.query('UPDATE zalish_advances SET "date" = issued_at::date;'); const r3 = await client.query('UPDATE zalish_expenses SET "date" = expense_date;'); console.log("Invoices updated:", r1.rowCount); console.log("Advances updated:", r2.rowCount); console.log("Expenses updated:", r3.rowCount); await client.query('COMMIT'); } catch(e) { console.error(e); await client.query('ROLLBACK'); } finally { client.release(); pool.end(); } } run();
